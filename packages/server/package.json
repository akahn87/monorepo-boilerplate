{
  "name": "@draxt/server",
  "version": "1.0.2",
  "main": "lib/index.js",
  "license": "MIT",
  "dependencies": {
    "apollo-server": "^2.12.0",
    "graphql": "^15.0.0"
  },
  "scripts": {
    "start": "nodemon --exec npx babel-node --root-mode upward --ignore=\" \" src/index.js",
    "test": "echo testing server with version: $npm_package_version"
  },
  "devDependencies": {
    "nodemon": "^2.0.3"
  }
}
